import styled from 'styled-components';

export default styled.div`
  .user--details {
    margin-top: 50px;

    .details--fullname {
      font-size: 22px;
      font-weight: bold;
      margin: 10px 0;
      color: #000;
    }

    .details--email {
      color: #000;
    }
  }

  .back-to-list {
    position: absolute;
    top: 30px;
    left: 30px;
  }
`;
